{
  "name": "schemebridge-monorepo",
  "private": true,
  "scripts": {
    "dev": "npm --prefix schemeBridge-frontend run dev",
    "build": "npm --prefix schemeBridge-frontend run build",
    "test": "npm --prefix schemeBridge-frontend run test"
  }
}
